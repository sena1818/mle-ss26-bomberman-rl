# Bomberman 项目交接：给写报告的人

这份文档说三件事：项目怎么组织、做了哪些实验和为什么、最终模型长什么样以及各模型怎么比。
每一节末尾都给出原始材料的位置，需要展开时按图索骥。

**一句话状态**：提交的是 `agent_code/psycho_killer/` —— M3 手工特征线的 Rainbow 配方，
四个训练种子的 Q 值在推理时平均，对手代理池均分 **6.2478**。

---

## 一、项目结构：五个彼此正交的维度

这个项目最重要的组织决定是：**把「一个实验」拆成五个可以各自独立取值的维度**。
不这样拆的话，一次同时改了状态表示、奖励、探索和对手，成绩变了也说不出是谁的功劳。
五个维度的名字在 `CONTEXT.md` 里冻结，全项目共用一套词汇。

| 维度 | 是什么 | 例子 | 不是什么 |
|---|---|---|---|
| **R 路线** | agent 的设计：状态表示 + 价值模型 + 更新规则 | `R02_11` = `handcrafted_v3` + C51 MLP + Double DQN | 不是奖励，不是课程 |
| **A 奖励版本** | 游戏事件到训练反馈的映射 | `A06` = A03 事件表 + 势函数塑形 | 不改变官方计分 |
| **E 探索时间表** | 训练期 ε 的值与曲线 | `E12` = ε 恒为 0；`E09` = 按环境步退火 | 不是路线的超参数 |
| **L 学习率时间表** | 步长的曲线 | `L01` = 5e-4 线性衰减到 1e-4，历时 2500 绝对回合 | — |
| **T 任务** | 场景 + 对手 | `T02` = `loot-crate` + 3 个 `rule_based_agent` | 不是路线 |

**为什么正交很重要**：跨维度的分数不可直接比较。同一个模型在 `loot-crate` 单人和
`classic` 三对手下的分数差好几倍，把它们并列就是错的。所以每个数字都必须写明
「哪个场景 + 有没有对手 + 什么对手」。

### 代码结构对应这个拆分

```
agent_code/research_agent/          一个 agent，四条路线共用
  callbacks.py / train.py           官方框架适配器，只委托，不含路线判断
  runtime/experiment.py             唯一的路线无关实现：select_action / observe / end_round
  state.py                          状态编码器（手工向量 v1/v2/v3、棋盘张量、混合）
  models/                           价值模型：linear_q / mlp_q / categorical_mlp_q / cnn_mlp_q / ensemble
  learners/                         更新规则：online_q / replay_q（DQN、Double DQN、C51）
  shaping.py                        势函数塑形
  config.py                         全部路线与五个维度的注册表
scripts/                            实验基础设施（见下）
```

**关键设计**：agent 是一个独立进程，从路线名 + `BOMBERMAN_*` 环境变量重建配置。
所以**每个声明的维度都必须跨过进程边界**——这一条踩过坑：学习率调度进了配置和快照
却没进环境变量，三个臂跑成了同一个调度，两个已汇报的结论作废。
现在有测试 `DeclaredAxesReachTheAgentTest` 守着。

### 实验基础设施

| 脚本 | 作用 |
|---|---|
| `run_experiment.py` | 把一个声明式 JSON 展开成训练 + 评估 job，每个 job 有私有工件目录与框架副本 |
| `aggregate_results.py` | 汇总，并按固定规则晋升模型 |
| `repeat_measure.py` | 重复测量协议：同一批 job 重跑 n 次，Welch 检验 |
| `tournament_pool.py` | **最终判定工具**：五张对手桌 × 棋盘配对（见第三节） |
| `package_submission.py` | 导出自包含的比赛 agent 并**真的跑一局**验证 |
| `diagnose_*.py` / `attribute_deaths.py` | 逐动作日志的离线诊断：逃生、死因、击杀机会 |

每个 run 记录 git commit、完整配置快照、以及拒绝从脏工作树启动。

**材料位置**：`CONTEXT.md`（词汇表）、`docs/00` §2（路线目录）、`docs/05` §8（代码落地）。

---

## 二、做了哪些实验、为什么、结果如何

完整时间线在 `docs/01` §7 与 `docs/05` §0（**后节会更正前节，引用时以最后一次更正为准**）。
下面按阶段走一遍，每一格给出「为什么做 / 结果 / 它改变了什么」。

**四条主线的定义**（取代最初设想的 R01–R08 八条路线）：

| 线 | 定义 | 回答的问题 |
|---|---|---|
| **M1** | `R01` 线性 + `A03` + `E01` | 最小可解释基线：状态、动作、奖励、评估管线对不对 |
| **M2** | M1 + 势函数 + n-step + replay，训练任务换成 `loot-crate` | 同一个模型，训练配方能榨出多少 |
| **M3** | M2 但模型换成 MLP | 手工特征之间有没有非线性交互 |
| **M4** | egocentric 棋盘张量 + CNN + Double DQN | 学到的空间表示能否超过手工特征 |

---

### 2.0 M0：基础设施与「先证明框架没读错」

**为什么**：所有后续数字的可信度都建立在「危险图和官方规则一致」上。

- 用 6 个单元测试逐项比对 `items.Bomb.get_blast_coords`：石墙阻断爆炸、
  **箱子会被摧毁但不阻断**、当前代码路径**没有炸弹连锁引爆**。
  这三条如果读错，整个逃生特征和诊断都会系统性偏。
- 每个训练/评估 job 有私有工件目录与**私有框架副本**——官方框架的日志路径是写死的，
  不隔离就会在并发 job 之间互相覆盖。
- 声明式 JSON 配置 + 展开成多个训练 seed × 多个独立评估 seed；
  长实验拒绝从脏工作树启动，worker 执行前再验一次 git commit。
- **训练代码不写共享模型文件**；只有聚合脚本能按固定规则晋升。

---

### 2.1 M1：线性基线，以及奖励设计的三次坍缩

**这一阶段最大的产出是三个「学不动」的失败，而且每个失败的机制都不同。**

| 臂 | 奖励 | 结果 | 机制 |
|---|---|---|---|
| A00 | 只有官方 +1/+5 | `classic` **0.00**；`coin-heaven` 29.68 | **信号饥饿**：1500 局只有 9 个金币事件（ρ⁺=0.006），greedy 退化成 `RLRLRLRL` 二周期 |
| A01 | A00 + 死亡 −5 | 全部 suite **0.000**，400 步不动 | **策略坍塌**：12,000 步评估里 **WAIT 11,992 次 / LEFT 8 次**——在有 50 枚可捡金币的图上完全冻结 |
| A02 | A01 + 炸箱 0.1 + 发现金币 0.2 | 450 局约一枚币 | 训练期确实变好（环境步 ×2.7、采币 ×11），**但 greedy 仍坍塌** |

**A02 还暴露了一个结构性问题**：`update_bombs` 在 `evaluate_explosions` 之前执行，
所以自杀那一步的事件里**同时**含 `CRATE_DESTROYED` 与 `KILLED_SELF`——
炸箱奖励在结构上无法区分「安全炸箱」与「炸死自己」，它只是把死亡惩罚削薄了。

**死亡惩罚剂量-响应（A02 D=5 / A03 D=1 / A05 D=0），canonical 数字**：

| | D=5 | **D=1** | D=0 |
|---|---:|---:|---:|
| official score | 0 | **0.140 ± 0.216** | 0 |
| `bomb_rate` | **0** | 0.0322 | 0.1877 |
| `suicides` | 0 | 0.289 | **0.976** |
| `steps` | 400 | 345 | **15.57** |

**两端都是 0 分，机制正好相反**：D=5 一发不放、站满 400 步；D=0 平均 **15.57 步**自爆。
只有 D=1 打破坍塌。这条剂量-响应经过三轮实现重跑都成立。

**同时撤回了一条**：预注册的可行性阈值 `p* = 0.689` 与实测 `p̂` 在前两轮「吻合」（0.693/0.702），
canonical 下是 **0.543**，差 0.146。**那个吻合不是稳定现象**，不能拿来验证均场模型。

**探索时间表 E00 vs E01 无法判定**：三种实现下效应符号是 **−7% / +71% / −48%**。
`500 局 × 5 seed` 的分辨率不够。

**一条影响后续全部设计的发现**：`terminal_on_truncation` 消融只改变**因步数上限截断**的
回合末那一条 transition——2500 个训练回合里只有 **10 个**，约占 80 万次更新的 **0.001%**。
就这点扰动把逐 seed 成绩完全重排（seed 1004 从 0.522 掉到 0，seed 1001 从 0.011 跳到 0.344）。
**所以这个预算下种子间差异不是「某些种子更好」，而是对微小扰动的敏感性。**
它统一解释了 E01 的三个符号、coin-heaven 的离群值、以及一步滞后能把存活步数从 15.6 变成 5.03。

---

### 2.2 两个阻断性缺陷（在做任何新实验之前必须修）

**P0：terminal transition 双重更新。** 存活到回合结束的 agent，最后一步会被
`game_events_occurred` 和 `end_of_round` **各交付一次**；而死掉的 agent 那条致命 transition
**只**从后者到达。无条件提交会双记，忽略后者会丢掉死亡惩罚。修法是用 `(round, step)` 去重。

修完之后**顺带引入了一步学习滞后**，又把它去掉，换成「立即提交 + 去重」。
**四个臂因此被重跑了三次**，三轮的关键数字并列保留在文档里——
这是「不覆盖已发表数字」这条规矩的由来。

**P1：curriculum × E01 会静默关闭 epsilon 衰减。** 课程的每个 segment 是独立进程、
回合计数从 1 重来，而 E01 的 hold 是**预算的百分比**，所以分段训练会永远停在最高探索期。
修法是显式的 `BOMBERMAN_ROUND_OFFSET` 与 `anneal_mode`。

**这两个缺陷共同的教训**：agent 是独立进程，**每一个声明的维度都必须跨过进程边界**。

---

### 2.3 M2：训练配方的四个单因素臂（模型仍是线性）

| # | 改动 | 关键结果 | 判定 |
|---|---|---|---|
| 1 | 训练任务 `classic` → `loot-crate`（T02） | `crates_per_round` **1.244 → 6.767**（×5.4），两个分布跨 seed **完全不重叠** | ✅ 预注册两条全过 |
| 2 | 势函数塑形 A06 | `suicides` 0.833 → 0.520、`wait_fraction` 0.323 → 0.249 | ⚠️ 核心安全预测（安全率 >0.85）**不通过**（实测 0.503） |
| 3 | n-step ∈ {1,3,5} | n=5 在几乎所有行为指标上优于 n=3，安全率 0.690 距门槛 0.70 差 0.010 | ⚠️ 未过 G2 |
| 4 | replay + target network | 安全率 **0.503 → 0.735**、`suicides` 0.520 → 0.391、存活 207 → 293 步 | ⚠️ 代价是放弹率 0.0999 → 0.0318 |

**实验 1 的代价定义了实验 2 的靶子**：任务密度让放弹行为稳定学出来了，
但学出来的是一个**不会躲的**放弹策略（安全率 0.450，过半的弹不安全）。

**M2 阶段没有出现稳定通过安全 gate 的臂。** replay 的安全改善是最稳的，
但它伴随放弹量下降——「更安全」有一部分是「更少放弹」。

---

### 2.4 M3.0 / M3.1：换成 MLP 本身让一切更差

| | M2 线性 n=5 | **M3.0 原始 MLP** | **M3.1 稳定 MLP** |
|---|---:|---:|---:|
| score | 1.433 | **0.567** | **7.047** |
| 安全放弹率 | 0.690 | **0.290** | **0.907** |
| `suicides` | 0.460 | **0.891** | 0.613 |

**只换模型（线性 → MLP 44-64-32-6）让每一项都退步。**
只有同时换上适配的更新配方（**Adam 1e-3 + Huber + 梯度裁剪 10 + replay + target network**）之后，
MLP 才体现出容量优势：0.567 → **7.047**，12 倍。

**这条要写进报告**：「换一个更强的模型」不是一个单因素改动，它连带需要换整套优化配方。

---

### 2.5 M3 状态表示：先量出缺什么，再加特征

**为什么**：M3.1 还有 0.613 的自杀率。先问它死在哪里，而不是直接加特征。

日志诊断（`diagnose_bomb_escape.py`，逐动作重放）的结果：
**在 94.4% 丢掉可生还逃生路的转向里，致命方向与救命方向在 `handcrafted_v1` 的危险特征里逐位相同。**
v1 只预测一步之后的危险，所以两个都在爆炸范围内的相邻格看起来完全一样，
即使一个通向出口、另一个是死胡同。

据此设计两个单因素臂：

| | `M3.1` v1 (44) | **`M3.2` v2 (54)** | **`M3.3` v3 (62)** |
|---|---:|---:|---:|
| 新增 | — | 多步逃生答案（10 维） | + 路由答案（8 维） |
| score | 7.047 | **12.653** | **14.671** |
| `crates_per_round` | 22.29 | 36.60 | **39.91** |
| `suicides` | 0.613 | 0.433 | **0.373** |
| **G2 六项** | 5/6 | **6/6** | **6/6** |

**这是全项目第一次出现 6/6 全过的臂。** 两臂的预注册项全部通过。

**一条必须并列的保留意见**：v3 的安全率（0.947）比 v2（0.964）**略低**。
路由特征改进的是**采集**，不是安全性——把它当安全改进引用是错的。

---

### 2.6 预算：一条被自己反复推翻的线

| 阶段 | 结论 | 后来怎么了 |
|---|---|---|
| §7.13 `M3.3L` 2000 局 | score 14.671 → **32.622**（2.22 倍），曲线到末尾**仍在加速** | — |
| §7.18 5000 局 | 预算与**探索时长纠缠**（E01 的 hold 是预算百分比） | 前面的预算结论全部混淆，`E02` 族改成绝对回合数 |
| §7.22 单独测量预算 | 效应**测不出** | 撤回「预算是最大杠杆」 |
| §7.23 训练更久反而更差 | 丢的是**击杀分**，不是采集能力 | — |
| §7.28 学习率衰减之后 | 曲线**不再饱和**（15.04 @2250 → 16.87 @4750） | 「预算不是杠杆」的前提是常数步长，重新打开 |
| §7.33 35 次重复 | 5000/7000 的区间夹到 `[−0.015, +0.081]` | 关闭 |

**安全放弹这项能力早在 1100 局就学会并饱和了，之后增长的全部是产量。**

---

### 2.7 对手：第一次让击杀有可能发生

**在此之前的 41 个配置里 `opponents` 全是空的**——§7.5–§7.13 的每一个数字
都产生于「击杀结构性不可能」的前提下。

三个单因素臂（单人 → 加对手 → 再换任务）：

| | 单人训练 | **加 3 个 `rule_based`** | 换成 `classic` 训练 |
|---|---:|---:|---:|
| 比赛 score | 2.087 | **2.227** | 1.389 |
| **存活到回合末** | **3.9%** | **17.4%** | 12.3% |
| 死于自己的弹 | **70.7%** | **46.9%** | 53.0% |

**official score 上几乎打平（2.087 vs 2.227），但存活率是 4.5 倍。**
只看分数会把一个 4.5 倍的行为效应报成「没有效果」。
**这是「行为指标比 official score 灵敏几个数量级」这条原则最强的单点证据。**

同时发现两个必须记的口径问题：
1. **`rule_based_agent` 不确定**（无参数 `np.random.seed()`），带对手的臂**不可复现**。
2. **跨「有无对手」的分数不可直接比较**——对手会抢走金币，
   所以引入 `coins_share`（我们拿到的金币 ÷ 全场金币）作为可比的竞争力指标。

**多人死因归因**（日志驱动全部四个 agent 精确重放，30/30 逐项复现官方统计）：

| M3.4 的 437 次自炸死亡 | 占比 | 性质 |
|---|---:|---|
| 放弹时就已无路可逃 | 6.6% | 多人**特有**（单人 0/11,224） |
| **全程有逃生方案却仍然死** | **38.2%** | 值函数问题 |
| 中途丢掉方案，且那一拍 `v3` 分辨不出 | **11.9%** | 表示不足 |

**「再加更强的手工逃生特征」的天花板只有 11.9%**（单人时同类占 66.9%，所以当年加 v2/v3 收益巨大）。
而最大的一类 38.2% 里，**92.1% 的救命方向明明白白写在 agent 的输入里，它选择了原地不动。**
**结论：不要再加手工特征了；剩下的是值函数问题。**

---

### 2.8 建立评估口径：本项目投入最大的一段

**起因是撤回过两次已发表的结论，两次都是把一次抽样当成结果。**

| 做法 | 数字 |
|---|---|
| 把同一个数测 20 次，量出噪声地板 | 15-job 池化分数 sd = **0.089** |
| ⇒ 两次单测能支持的最小差 | **0.246** |
| ⇒ 可分辨门槛 | `\|t\| > 2.3` |
| 门槛可以用样本量买下来 | 重复 n 次，标准误降到 1/√n |
| 棋盘难度是最大方差项 | 跨棋盘 sd 0.29–0.34，跨训练 seed < 0.10 |
| ⇒ 配对到棋盘种子 | 6 个配对 ≈ 12 个非配对，机时减半 |

**一个具体的例子说明为什么必须重复测量**：`M3.12` 相对 `M3.4` 单次测量 `+0.189`，
判为不可分辨；重复 5 次之后真实的 `+0.113` **却可分辨**——标准误降到三分之一。

**选种偏差也量过**：`holdout_metrics` 是在 `seed × round` 上取最大的那一个，
换成五 seed 池化的重复测量后，dueling 从 3.8111 **掉到 3.0331**、anchor 从 3.2556 掉到 2.8504，
而 opponents 几乎没动（3.8444 → 3.8464）。
**偏差在方差大的臂上大，不是一个可以统一扣掉的常数——只能逐臂重测。**

---

### 2.9 M3 单因素臂族：探索、算法、容量

六个臂，唯一改动各不相同，对照是同一个（分辨率门槛 4.7，写在配置里、跑之前定死）：

| 臂 | 唯一改动 | Δ | 判定 |
|---|---|---:|---|
| `M3.6A` | hold 400 → 100 | −3.10 | 不可分辨 |
| `M3.6B` | hold 400 → 1000 | −3.09 | 不可分辨 |
| `M3.6C` | floor 0.05 → 0.02 | −0.98 | 不可分辨 |
| **`M3.6D`** | floor 0.05 → **0.10** | **−8.61** | ✅ 可分辨**更差** |
| `M3.7` | `q_learning` → `double_dqn` | −0.85 | 不可分辨 |
| **`M3.8`** | 隐层 (64,32) → **(128,64)** | **−5.72** | ✅ 可分辨**更差** |

**探索时间表只有一个可分辨的效应，而且是负的**：把 floor 抬到 0.10 让分数掉 8.61，
机制在 `crates_per_round` 上看得见（83.08 → 62.34）——**它炸得少了**。
现有的 `hold=400 / floor=0.05` 在四个方向里点估计最高，不需要改。

**「加容量有害」后来被证明是配方伪影**——这是本项目最值得写的一次自我更正：

| 宽度 | 学习率 | 算法 | score | Δ |
|---|---|---|---:|---:|
| (64,32) | 1e-3 | q_learning | 32.622 | — |
| (128,64) | 1e-3 | q_learning | 26.904 | −5.72 ✅有害 |
| (128,64) | 1e-3 | double_dqn | 24.882 | −7.74 ✅有害 |
| (128,64) | **5e-4** | q_learning | 31.576 | −1.05 ❌ |
| **(128,64)** | **5e-4** | **double_dqn** | **33.093** | +0.47 ❌ |

**同一个组合只差学习率是 `+8.21`，远超门槛。**
`mean_hidden_zero_fraction` 跟着**学习率**走而不是跟着宽度走
（1e-3 的两个宽臂 0.791/0.790，5e-4 的两个 0.659/0.657）——它是步长的签名，不是容量信号。

**逃生诊断进一步确认**：每千弹死亡在四个 1e-3 的臂上是 11.3–17.9，
两个 5e-4 的臂上是 **6.4–6.6**。**宽度和算法的效应都排在学习率之后。**

**顺带撤回了一个判定指标**：「撤离拍 WAIT 比例」不预测安全性——
死亡率最低的两个臂 WAIT 比例最高，最高的那个只有 7.41%。
今后的安全判定用**每千弹死亡**和**全程有方案却死**的绝对计数。

**学习率调度本身的三个旋钮也各测了一遍**（比赛口径，各十次重复）：

| 旋钮 | 改动 | Δ | t | 判定 |
|---|---|---:|---:|---|
| 底值 | 衰减到 20% → **5%** | +0.032 | +0.65 | 测不出 |
| 形状 | 线性 → **余弦**（端点与长度不变） | −0.001 | −0.02 | 测不出（**全项目最接近零的效应**） |
| **长度** | 2500 局 → **铺满 5000 局** | **−0.354** | **−6.86** | ✅ 可分辨**更差** |

**三个旋钮里只有长度有效应，而且是负的。** 现有的 `L01`（5e-4 → 1e-4 / 2500 绝对回合）
不需要改；原本排队的两个更长调度臂因此取消。

---

### 2.10 学习率调度：本线最大的单项效应

`R02_9` **相对 F2 只差 `learning_rate_schedule` 一个字段**（`L00` 常数 → `L01`：
5e-4 线性降到 1e-4，历时 2500 **绝对**回合，之后保持）。

| 十次重复，比赛口径 | score | `coins_share` |
|---|---:|---:|
| `M3.13` | 2.4573 | 0.2419 |
| F2 | 2.5396 | 0.2548 |
| **`R02_9`** | **3.6371** | **0.3449** |

**`R02_9` vs F2：`+1.0976`，`t = +20.2`。十次全部（3.402–3.904）高于另外两臂的全部（2.256–2.711），完全不重叠。**

**但我的机制读法是错的**：我预测「步长过大在最优附近打转，衰减会把它收进去」，
预注册的判定指标是 2500–5000 段的标准差。标准差确实降了（0.5213 → 0.4144），
**但真正的效应在均值上**，而且曲线不是收敛——**是不再饱和**。
**这是被数据修正的第七个机制假设。**

**它还重新打开了两条已关闭的结论**：「对手训练 2250 局饱和」和「预算不是杠杆」
的全部测量都用常数步长，在衰减步长下需要重测。

---

### 2.11 击杀线：三次尝试，三次关闭

**先量清楚缺口有多大**。F2 的 25,000 个训练回合的事件统计：

| 事件 | 每局 | 权重 | 占正奖励总量 |
|---|---:|---:|---:|
| `COIN_COLLECTED` | 6.27 | 1.0 | 66.1% |
| `CRATE_DESTROYED` | 16.88 | 0.1 | 17.8% |
| `COIN_FOUND` | 6.85 | 0.2 | 14.4% |
| **`KILLED_OPPONENT`** | **0.033** | **5.0** | **1.7%** |

**奖励表里最大的那一项只占正奖励的 1.7%，比金币稀疏 191 倍。**

**再量它缺哪一半**（900 回合，日志驱动重放，30/30 复现）：

| | 占比 |
|---|---:|
| 存活步中手里有弹 | 64.6% |
| **此刻放弹能炸到对手** | **9.9%**（占持弹步） |
| └ 放完还能安全撤离 | **97.9%** |
| └ **agent 实际放了** | **8.0%** |
| `rule_based` 自己的触发条件下我们放弹 | 6.6%（它自己接近 100%） |

**它走得到位置、97.9% 的机会放完还能逃，而它只放 8.0%。这是决策问题。**

三次尝试全部关闭：

1. **A07 击杀塑形**：在正确对照下采纳率**确实翻了一倍**（6.8% → 12.2%），
   而击杀 0.1149 → 0.1151（`t = +0.03`）。**边际转化率 0.02%：多放 809 弹换来 0.2 次击杀。**
2. **弱对手课程**：同一份权重对不躲的 `peaceful` 是 **0.562** 次/局，对 `rule_based` 是 0.107。
   **要学的已经会了；低击杀率不是我们不肯开火，是 `rule_based` 是框架里躲得最好的东西。**
   而围杀需要对手**会逃**，三种弱对手没有一个会逃。
3. **击杀信号密度**（最近一轮，见 2.14）：预测命中但零和，且对照证明不是击杀密度的功劳。

**顺带量到一件事**：`rule_based` 的击杀率是我们的 1.66 倍，但三个的总分**全都比我们低**——
它们用金币换击杀，换亏了。而且它们每局合计 0.534 次击杀里，**只有 0.126 落在我们身上，
其余 76% 是它们互相残杀**。

---

### 2.11b 三条被干净关闭的线

**n-step**：M2 阶段的 `n=5` 是在**线性模型、500 局**上选出来的。
在 `(128,64) + double_dqn +` 对手训练下重扫 `n ∈ {3, 5, 8}`：两端都不可分辨，
**n=5 的选择迁移过来了，这条线关闭**（后来在新配方下 n=3 是可分辨更差 −0.286，`t = −19.7`）。

**两阶段微调**——这是唯一一条**指向评估任务本身**的方向，所以值得跑，
也是「别从旧 checkpoint 续训」这条硬规矩的显式例外：

| 十次重复，比赛口径 | score | t vs `R02_9` |
|---|---:|---:|
| `R02_9`（基线） | 3.6371 | — |
| 接缝对照（第二阶段留在 `loot-crate`） | 3.6682 | +0.49 ❌ |
| **在 `classic` 上微调** | **3.4304** | **−3.77 ✅ 更差** |

**接缝本身不是原因**（对照 `t = +0.49`）；**是场景**。五个剂量上 `classic` 全部更差，
机制是「更安全、更不产出」（`suicides` −0.035、coins −0.224）。
更反直觉的是：**在 `classic` 上微调的 agent，连 `classic` 单人都更差。**

**提交选择的一次两难（值得写进报告的方法论）**：F2 相对 `M3.13` 的 score 是 `+0.082`（`t = +1.64`，
不可分辨），但 coins（`t = +4.27`）、`coins_share`（`t = +4.29`）与存活率（14.3% vs 2.8%）都可分辨。
**预注册的规则字面指向交 `M3.13`。** 文档的处理是：不替这个选择下结论，
但要求「若改交 F2，必须写明这是事后把标准从『分数最高』改成了『分数打平时按稳健性选』」。

顺带一个不能忽略的细节：**加样本没有把 score 推过线，反而拉回来了**——
`t` 从 n=5 的 1.96 → n=7 的 1.80 → n=10 的 1.64，差值从 +0.110 缩到 +0.082。
**效应比早期估计更小，不是样本不够。n=10 是事先说定的样本量，到此为止；
继续加到显著为止是样本量凑数。**

---

### 2.12 算法族：C51 / PER / Rainbow

| 臂 | score Δ vs `R02_9` | t | 判定 |
|---|---:|---:|---|
| C51 分布式头（单独） | +0.024 | +0.46 | 不可分辨（`suicides` 可分辨更低但**一枚币都没换到**） |
| PER（max 归一化） | −0.194 | −3.79 | ❌ 更差 |
| PER（mean 归一化） | −0.233 | −5.13 | ❌ 更差 |
| PER @ 10000 局 | −0.082 | −1.73 | 追平，净收益零 |
| C51 @ 7000 局（组合） | −0.028 | −0.54 | **不叠加**：预算的效应换了个价值头就反号 |
| **Rainbow 六件套** | **+2.160** | **+42.14** | ✅ **本线第一个可分辨更好的臂** |

**Rainbow 的 +2.160 里 95% 是击杀**（0.108 → 0.520），金币只涨 0.10。

**归因做了一半**：四个成分里三个已有单因素臂且都没赢（C51 不可分辨、PER 更差、预算 t=−0.27）。
剩下 noisy 与 dueling，而**dueling 在本仓库做不出单因素臂**（它的 V/A 拆分写在 C51 的 logits 上）。
所以对 noisy 做了「加一进去」检验（见 2.13），**结果证伪了 noisy 的解释**。

**击杀从哪来**（两个假设被自己的对照推翻）：不是「更敢按 BOMB」（A07 的下手率已接近 Rainbow 而击杀没涨），
也不是「炸被逼到死角的对手」（那种情形只有 104 次，解释不了 371 次击杀差）。
真正的答案是：**Rainbow 的击杀 61% 来自放弹时没有目标的「盲弹」**——
提前布下的弹在五步之后把人困死。每 100 颗盲弹的命中率是 `R02_9` 的 **4.4 倍**。
**这需要的是对手未来位置的判断，而 A07 买的是「按下 BOMB 这个动作」——所以塑形买不到它。**

---

### 2.13 ε≡0：一个 bug 做出的真结果

`R02_12` 与 `R02_13` 各训了 10000 局，**声明了 noisy 层、通过了校验、写进了快照，
而 checkpoint 里一个 σ 参数都没有**——`build_model` 的 `mlp_q` 分支没把 `config.noisy` 传下去。

噪声不在，两个臂就退化成一个**干净的单因素对**：同一份代码、同一批 seed、同一天，
**只差 ε**。判定（十次重复）：

| ε≡0 @10000 对 | Δ | t |
|---|---:|---:|
| E02 调度 @10000（同日新 run） | +0.1982 | **+4.50** |
| E02 调度 @10000（旧 run） | +0.2429 | **+6.64** |
| `R02_9` @5000（线基线） | +0.2307 | **+4.81** |

**三个对照方向一致，两个是同预算单因素。** 分项：金币 +0.2107、`coins_share` +0.0232、
自杀 −0.0527，**击杀不变**。

**读法：ε 的随机动作在这个环境里是净损害，而它损害的是金币与存活，不是击杀。**
而且 ε≡0 的五个 seed 跨度只有 **0.1033**，是全线最稳的臂。

**后来补跑的真噪声臂证伪了 noisy 的解释**：预注册预测击杀落在 0.25–0.53，实测 **0.1289**。
**所以 Rainbow 的击杀不是 noisy 探索给的**，功劳在 dueling 或交互作用。

---

### 2.14 迁移：Rainbow 的优势有多少是过拟合

**为什么做**：锦标赛的对手是别的小组的 agent，不是 `rule_based_agent`。

同一份权重换对手（12 个棋盘种子，配对）：

| 对手 | Rainbow / `R02_9` 的击杀倍数 |
|---|---:|
| `rule_based`（训练见过） | **4.91×** |
| 冻结 `R02_9` | 1.11× |
| `coin_collector` | 1.26× |
| 冻结 Rainbow（打自己） | 1.87× |

**击杀优势是对手特异的。** 而金币是**唯一稳定迁移的技能**。

**这里也撤回过一次**：初版用 3 个棋盘种子测，得出「击杀优势归零」和
「ε≡0 分数上赢 Rainbow」。加到 12 个之后：**击杀是掉到 1.1–1.9 倍而不是归零**，
而**分数上 ε≡0 并没有赢**。
**教训与前八次不同**：前八次是机制说错了，这一次是**数字本身不够精确就被引用了**。
对确定性对手，「十次重复」不产生任何信息——样本量是**棋盘种子数**，`sd = 0.0000` 是唯一的线索。

---

### 2.15 M4：学习式空间表示线，从头到尾

**为什么值得做**：诊断量出 `handcrafted_v3` 在比赛设置下有 **34.7%** 的致命转向不可分辨
（单人是 0%）。**手工线在训练任务上已无可测的表示瓶颈，在比赛设置上还有。**

**启动前修掉的六处协议缺陷**（在动用二十多个 CPU 小时之前）：

| 问题 | 修法 |
|---|---|
| 吞吐**低估 2.1 倍** —— benchmark 只计时 `fit_batch`，而 Double DQN 一次更新的大头是两次 next-state 前向 | 改测完整 learner 路径：**20.34 ms/梯度步，不是 9.59** |
| 「调用 learner」与「真的做了梯度」共用一个计数器 | 分开报告 `gradient_applied` / `gradient_steps` / `replay_size` |
| `coins_share` 是判据，聚合器根本没算它 | 加入指标集 |
| checkpoint 选择在 `train_seed × round` 上取最大 | 先按 round 池化 → 在 **validation** 曲线上选 → 冻结 → 才读 holdout |
| 「holdout 曲线必须单调上升」 | 改成 validation 上的窗口趋势；holdout 完全不参与 |
| 一臂 1265 个 job | 显式指定评估轮次，**一臂 335 个 job** |

**探索时间表被改了两次，两次都是被 pilot 推翻的：**
- 所有旧时间表从 ε=0.30 起步——这在线性/小 MLP 上从不是问题，但**随机初始化的 CNN 的
  argmax 在所有状态上近乎恒定**，0.30 意味着 70% 的步在执行同一个固定动作。
  smoke 直接测到 `wait_fraction` **0.93**、`distinct_cells` **1.78**。
- 改成「ε=1.0 保持 100 **回合**」（E07）之后，pilot 测出 hold 内只攒到 **982** 条 transition，
  而 `min_size` 是 10,000——**第一次梯度落下时（round 826）ε 已经退到 0.11，冷启动没了**。
  根因是**单位不一致**：时间表按回合计，buffer 按 transition 计，
  而一个回合的长度在一次 run 内变化 **20 倍**（ε=1 时 9.8 步，学会后 210 步）。
- 最终 `E09`/`E10` 按**环境步**计数，`hold_steps = min_size` 是刻意的——
  第一次梯度恰好在 buffer 装满时发生，且此时 ε 仍是 1.00。

**七个臂的第一批结果**（每臂 5 训练 + 330 评估 job）：

| 臂 | 相对 anchor 的唯一改动 | 比赛口径（重复测量后） |
|---|---|---:|
| anchor | 基座 | 2.8504 |
| lr 1e-4 / 5e-4 | 只换学习率 | — |
| A03 去塑形 | 只去掉势函数 | loot-crate 46.71 vs 46.55 **不可分辨** |
| **对手暴露** | 只加 3 个 `rule_based` | **3.8464 ✅ 赢 M3** |
| BC 热启动 | 初始权重 + ε 起点 | round 2000 时 **43.80 对 0.47** |
| dueling | 只换 head | 3.0331 |

**四条站得住的结论：**

1. **CNN 不需要手工 BFS 势函数**——去掉 `potential_v1` 后分数不动，
   而 M3 线上同样的改动是**可分辨更差**。「学习式空间表示」这个论述成立。
2. **BC 热启动买下了约 4000 回合**，且是逐 seed 最稳的臂。
3. **步长的效应是稳定性，不是均值**：`lr5e4` 的均值被**一个发散的 seed** 拉低
   （`{2.30, 43.49, 43.40, 43.93, 48.49}`）。**5e-4 不是均匀更差，是五分之一的概率发散。**
4. **没见过对手的臂在有对手时崩溃**：anchor / dueling 的自杀率是 0.584 / 0.605，
   而训练时见过对手的那一臂只有 **0.158**。**这是分布外表现，不是这条线的性质。**

**死因归因**（对手臂 vs 另外两臂，每臂 900 回合）：

| 每回合 | **opponents** | anchor | dueling |
|---|---:|---:|---:|
| **活到回合结束** | **78.6%** | 26.4% | 21.4% |
| 死于自己的炸弹 | 13.2% | 49.9% | 57.6% |

**对手臂不是「少自杀」，是整体上几乎不死。**

**迁移测量**（换对手，配对到 12 个棋盘）给出一条**单因素硬结论**：
`anchor` 和 `opponents` 有**完全相同**的表示、算法、预算，唯一差别是训练时见没见过对手，
而 anchor 是全场最差（2.3739，低于每一个 M3 臂）。
**所以 CNN 表示本身不带来迁移能力；把它训练在有对手的分布上才带来。**

**最后一步 `opponents + BC`**：预注册的预测原样命中——冷启动臂在 round 2000 是 0.0144，
热启动的已经 3.55。而且**热启动没有改变存活**（`t = +0.16`，完全打平），**它加的全是金币**
（+0.0947，`t = +3.74`）。这不是事后解释，是先写下来再量到的。

---

### 2.16 最近一轮：代理池、集成、以及击杀密度

见第 2.8 节末与下面三条：

**（a）代理池取代 `rule_based` 成为选型口径**，并在两组独立棋盘上复现了自己。

**（b）集成**：同一条臂五个种子的 Q 值平均。三条臂全部可分辨受益（+0.46 到 +1.17）。
规模曲线在**三个成员就饱和**（+0.861、+0.501、然后是零）。
留一法（在选种棋盘上选、在报数棋盘上报）选出四成员组合，**6.2478**。

**（c）击杀信号密度**：见 2.11 第 3 条与下面的负面结果小节。

---

### 2.17 三个带对照的负面结果

这三个是报告里最能体现方法论的材料，**因为没有对照就会得出相反的结论**。

**（a）击杀信号密度。** 假设：把训练对手换成好杀的，击杀信号变稠密，agent 学会杀人。
**假设完全命中**——剂量单调，最高一档（3 个 `peaceful`）把 CNN 的击杀带到 0.358，
正好追平 Rainbow 集成的 0.359。**但它是零和的，算术精确闭合**：

```
击杀 +0.210 × 5 =  +1.050
金币 −1.152 × 1 =  −1.152
                   ───────
净               =  −0.102     实测正是 −0.102
```

**对照臂证明损失不来自击杀密度**：单独隔离后两条线上分别是 t = −1.68 与 t = +1.37，符号相反。
真正的损失是共有的——把 `rule_based` 换掉，训练分布里的**炸弹**也一起没了。

**（b）为造「中间档」对手写的 `evader_agent`。** 会躲、不放弹、不抢金币。
**实测比 `rule_based` 更难杀**（0.075 对 0.200），加警觉度旋钮也没用。
机制写在对手自己的数字里：`rule_based` 每局自杀 **1.27** 次，`peaceful` 与 `evader` 都是 **0.00**。
**让 `rule_based` 送命的是它自己放弹。** 这个失败的设计最后成了（a）的对照臂。

**（c）续训的接缝。** 在**完全不变**的桌子上续训 5000 局，仍然输源臂 0.468（t=−7.99）。
数值复现了 M3 线上的 −0.45，但那次说「1000–2000 局内还清」，M4 线上 5000 局**没有**还清。

**另外两个已关闭的方向**：自对弈（三个相同的冻结对手，−0.508，t=−11.5，
金币份额按对称性坍缩到 0.289）；显式正则化（权重衰减 1e-4，−0.102，t=−2.55）。

---

### 2.18 被量出来的天花板

**金币的上限是视野，不是算法。** 把 450 局里每一枚金币追出来：

| 金币现身时的曼哈顿距离 | 我们抢到的胜率 |
|---|---:|
| 0–2 | 89.2% |
| 3–4 | 82.8% |
| **5–6** | **72.4%** |
| **7–8** | **24.1%** |
| 9+ | < 19% |

**一格之差，胜率从 72.4% 掉到 24.1%。** 断崖是物理的——金币从露出到被捡走中位 **7 步**，
移动每步一格。每局露出 8.16 枚里有 **4.88 枚**在露出那一刻就已在六格之外。
六格内做到接近完美是 **3.6 枚**，我们已经在 3.1–4.1。

**而且棋盘每局都被刮干净**：四个 agent 每局合计收集 8.97 / 9 枚金币，
炸掉 122 / 约 123 个箱子，**比赛在前 135 步内就结束了**。
所以后期「站着不动」不是缺陷——那时已经没有东西可捡，而开阔地上无法围堵。

**我们在每一个转化环节上都是四个里最好的**：每弹炸箱 2.10（对手 1.45–1.58）、
每箱出币 0.0880（0.0644–0.0707）、自己炸开的金币自己拿到 87.0%（65.6–69.2%）。
**唯一落后的是放弹次数。**

---

### 2.19 九次撤回的机制读法，与四个「声明了却没到达模型」的缺陷

**九次**从代码或数据推理得出、听起来完全合理、却被实测推翻的假设：
势函数偏向 WAIT、对手堵死逃生路、高估导致 WAIT、容量不足、预算是最大杠杆、
塑形能把 5.0 传回放弹动作、震荡是在最优附近打转、Rainbow「更敢按 BOMB」、
Rainbow「炸被逼到死角的对手」。**共同点都是先有故事、后有数据。**
（第十次性质不同：不是机制说错，是**数字本身不够精确就被引用了**。）

有效的做法一直是反过来：`v2`/`v3` 之所以成功，是先量出了 **94.4%** 才动手；
`A07` 之所以能干净关闭，是先量出了 **0.02%** 的边际转化率。

**四个「声明了却没到达模型」的缺陷**，这类 bug 最糟的形态是什么都没错、出错时也不会有人知道：

| 缺陷 | 后果 |
|---|---|
| 学习率调度没进环境变量 | 三个臂跑成同一个调度，**两个已汇报的结论作废** |
| `build_model` 没把 `noisy` 传下去 | 两个臂各训 10000 局，checkpoint 里**零个 σ 参数** |
| torch 优化器构造时就复制了 `lr` | 调度赋值到对象上**到不了优化器**，`L01` 臂会跑完全程测出精确的零 |
| 势函数危险项在 n≥5 时贡献**精确为零** | 炸弹寿命恰好 5 个 transition，望远镜求和两端相消 |

四处现在都有 fail-closed 检查或断言守着。

**还有一次归档事故**：本地一个 run 目录里装着**另一个臂**的结果，而文档拿它当对照引用过。
成因是同步流程里 run 名与目录名可以不一致。两条流程修复：
`verify_run_archives.py` 比对每个 `evaluation_summary.json` 里记录的 `run_id` 与目录名
（当前 151 个 run 全部名实相符），以及 `run_ledger.py` 从每个 run 自己的快照生成索引，
**不是手写的，所以不会与磁盘漂移**。

**由此形成的八条硬规矩**（写在 `docs/M3_BASELINE_R02_9_PROMPT.md`）：

1. `prepare` 要求干净的已提交 checkout，每个 job 执行前再验一次；**服务器上有 job 在跑时绝不 `git pull`**
2. 本机跑长实验用 `git worktree add --detach`，避免与其它会话冲突
3. 凡经过选种的数字，**必须同时报告全部 training seed 的分布**
4. 别从旧 checkpoint 续训（两阶段微调是唯一的显式例外，须标注）
5. 写文档**保留原表、并列新表**，在旧节顶部加指向新节的提示，**不要覆盖已发表的数字**
6. 跑完压缩日志，再拉轻量副本回本机
7. 拉完归档跑 `verify_run_archives.py`
8. 每个分数写明「哪个 scenario + 有没有对手」，跨这两个维度的数字不并列

---

## 三、最终模型：架构、指标与对比

### 3.1 提交模型 `psycho_killer`

**路线 `R02_11`**，四个训练种子（1002/1003/1004/1005）的 Q 值在推理时平均。

| 层面 | 配置 |
|---|---|
| **状态表示** | `handcrafted_v3`，**62 维** |
| | v1（44）：自身/全局 5、六动作立即合法性 6、当前与四邻格的预测危险 10、最近可达金币/箱子/对手各 4、最近炸弹 5、半径 2 内四类计数 4、放弹后逃生能力 2 |
| | v2（+10）：`escape_here` 2、`escape_by_direction` 8 —— 多步逃生搜索的答案 |
| | v3（+8）：`coin_route` 4、`crate_route` 4 —— 哪个合法动作真的缩短了路程 |
| **网络** | MLP 62 → 128 → 64 → 头，ReLU，He 初始化 |
| **价值头** | C51 分布式：支撑 `[-2, 12]` 上 51 个原子；dueling 拆分写在 logits 上，`(6+1) × 51 = 357` 输出 |
| **探索** | Noisy 层（σ 初始化 0.5/√fan_in）取代 ε-greedy，`E12` 即 ε 恒为 0 |
| **可学习参数** | 约 **79,050** |
| **算法** | Double DQN，n-step **5**，优先经验回放（容量 50k、batch 32、min 1000、target 每 500 梯度步同步、β 从 0.4 退火到 1） |
| **优化** | Adam 5e-4 **线性衰减到 1e-4，历时 2500 绝对回合**（`L01`），交叉熵损失，梯度裁剪 10（全程未触发） |
| **γ** | 0.95 |
| **奖励 `A06`** | `COIN_COLLECTED` 1.0、`KILLED_OPPONENT` 5.0、`CRATE_DESTROYED` 0.1、`COIN_FOUND` 0.2、死亡 −1.0（每次死亡计一次，不是每个事件标签一次）+ 势函数 `potential_v1`（金币权重 0.05、距离上限 20、危险权重 0.30） |
| **训练** | `loot-crate` + 3 × `rule_based_agent`，10000 局 × 5 个种子，全部 fresh start |
| **集成** | 四个成员的 Q 值按元素平均后取 argmax |

**为什么平均价值而不是投票**：投票丢掉「每个成员有多确信」，而在 Q 函数上量级是校准过的那一半。
测试里钉了一个例子：两个轻微偏好 UP 的成员加一个强烈偏好 BOMB 的成员，投票选 UP，值平均选 BOMB。

**集成为什么有用**：单个 Rainbow 的自杀率 0.269，是 M4 那条线的 5 倍——
自杀来自「过度自信的放弹」，某一个网络因训练噪声在某个局面高估了放弹价值。
五个网络里只有一个这么想时，平均下来这个动作不再领先。而围杀所需的判断是多个成员共有的。
所以**自杀率减半（0.269 → 0.138）而击杀反而上升（0.280 → 0.359）**。

**集成规模是量过的**：收益全在前三个成员（+0.861、+0.501），第四第五个是零。
所以没有再训更多种子。第四个成员是**挑**出来的——留一法在选种棋盘上选，
在报数棋盘上报，选中「去掉种子 1001」。

### 3.2 第二个 agent：M4 学习式空间表示线 `R07`

课程要求至少两个不同的 ML agent，两个都要写进报告。

| 层面 | 配置 |
|---|---|
| **状态表示** | `board_egocentric_v2`，**7 × 17 × 17 + 6 = 2029 维** |
| | 七个通道：石墙、箱子、对手、金币、炸弹倒计时、当前爆炸、由已知炸弹推导的未来危险 |
| | 六个全局标量：`can_bomb`、回合进度、自己分数、存活对手数、可见金币数、箱子占比 |
| | 以 agent 为中心裁剪，界外补石墙——所以整体平移对网络不可见，八重对称群可作用其上 |
| **网络** | Conv(7→32, 3×3) → Conv(32→64, 3×3, stride 2) → Conv(64→64, 3×3, stride 2) → Flatten(1600) ⊕ MLP(6→32) → FC 256 → 6 个 Q 头 |
| **可学习参数** | **477,286** |
| **算法** | Double DQN，n-step **3**，均匀回放（容量 100k、batch 32、min 10k、`train_every` 4 即回放比 8）、target 每 500 梯度步同步、**D4 八重对称增强** |
| **优化** | Adam 2.5e-4，Huber，裁剪 10 |
| **热启动** | 行为克隆 150,000 个状态，验证准确率 0.880 |
| **探索** | `E10`：ε 从 0.20 起，保持 10,000 **环境步**后在 50,000 步内退到 0.05 |
| **训练** | `loot-crate` + 3 × `rule_based_agent`，10000 局 × 5 个种子 |

**两个实现细节值得写**：
- 回放缓冲用 **uint8** 存棋盘（所有通道值都落在 1/20 网格上，编码精确可逆）。
  两份 float32 的 2029 维状态是 16.2 KB/条，100k 就是 1.6 GB/job；uint8 是 4.1 KB、406 MB。
- **n 必须留在 3**：势函数的危险项对 n-step target 的贡献是望远镜求和，
  而炸弹的可观测寿命恰好是 5 个 transition，所以 **n ≥ 5 时该项对放弹决策的贡献精确为零**。

### 3.3 各模型对比（对手代理池，报数棋盘 4007–4012，五桌等权）

| 模型 | 成员 | score | coins | kills | suicides |
|---|---|---:|---:|---:|---:|
| **`psycho_killer`（提交）** | 4 | **6.2478** | 4.109 | 0.428 | 0.129 |
| M3 Rainbow | 5 | 5.9056 | 4.111 | 0.359 | 0.138 |
| M4 对手暴露 + BC | 5 | 4.8933 | **4.154** | 0.148 | **0.073** |
| M3 ε≡0 | 5 | 4.8467 | 4.174 | 0.134 | 0.121 |
| M3 Rainbow | 1 | 4.7318 | 3.333 | 0.280 | 0.269 |
| M4 对手暴露 + BC | 1 | 4.4369 | 3.749 | 0.138 | 0.120 |
| M3 ε≡0 | 1 | 4.2284 | 3.641 | 0.118 | 0.255 |
| M3 Rainbow 混合桌训练 | 1 | 3.1456 | 2.513 | 0.126 | 0.387 |

**三条读法**：

1. **集成对每一条臂都是可分辨的提升**（+0.46 到 +1.17），而且对 Rainbow 最大。
2. **两条线的优势正交**：Rainbow 赢在击杀（0.428 对 0.148），
   M4 赢在存活（自杀 0.073 对 0.129）与金币。这一点在逐桌数据上更清楚——
   Rainbow 在对手不犯错的桌上赢，M4 在对手会犯错、场上没人可杀的桌上赢。
3. **在 `rule_based` 上比大小会得出错误结论**：Rainbow 打 3×`rule_based` 是 5.7975，
   但那 +2.16 里 95% 是击杀，而**换任何没见过的对手，击杀优势从 4.91 倍掉到 1.1–1.9 倍**。
   这就是为什么最终判定用代理池而不是 `rule_based`。

### 3.4 提交包的工程验证

| 检查 | 结果 |
|---|---|
| 在只含框架和这个目录的临时树里打 15 局 | 通过 |
| 剥掉全部 `BOMBERMAN_*` 环境变量 | 通过（路线与权重钉在 `submission.json`） |
| 最慢决策 | **0.90 ms**（官方预算 500 ms，**554 倍**余量） |
| 超时次数 | 0 |
| 是否需要 PyTorch | **否**，只依赖 numpy |
| 体积 | 1.6 MB，37 个文件 |

**一个必须写进报告的坑**：路线原本从环境变量读、默认回退是 `R01` 线性基线。
组委会只导入 `callbacks.py` 且不设任何环境变量，
**不修的话提交的模型会安静地以最早的线性 baseline 身份下场**，不会报任何错。

**材料位置**：`docs/01` §7.44（ε≡0 配方）、§7.39（Rainbow）、
`docs/05` §0.36（M4 提交候选）、§0.40（集成）、§0.43（集成规模与留一法）、
`docs/06` §6（M4 最终配方）。

---

## 四、这个包里有什么

```
HANDOVER.md                 本文档
docs/                       完整实验记录，11,794 行，438 个小节
                            docs/01 每个臂的表格与判定 · docs/05 时间线与更正 · docs/06 M4 设计
experiments/                90 个实验配置。每个的 _design_note 与
                            _predeclared_design_numbers 记录了跑之前写下的预测与作废条件
diagnostics/                37 份离线诊断（逃生、死因归属、击杀机会、金币追踪）
results/                    所有对手代理池测量的汇总（JSON + 排行榜）
code/                       agent 实现与实验基础设施
submitted_agent/            psycho_killer，可直接放进 agent_code/ 运行
models/                     被引用过的 checkpoint（提交模型的四个成员 + 三个对照）
```

**没有放进来的**：逐动作日志（服务器上 59 GB）。
所有依赖它的诊断已经跑完并写进 `diagnostics/`，结论在文档里。
需要重跑某个诊断时再从服务器拉那一个 run。

**怎么跑一局看看**（在框架根目录）：

```bash
cp -R submitted_agent/psycho_killer agent_code/
python main.py play --my-agent psycho_killer
```
