# 路线重构与实验排期

> 状态：2026-08-26 v2。**§1、§2 与 §6 的并行化已实现并验证；§3–§5 仍是待审核提案。**
> 触发原因：发现并证实了一个 terminal-transition 语义 bug（§1），它会污染所有跨 arm 的定量比较。
> 与既有文档的关系：不改写 [01](01_实验路线_奖励_课程训练设计.md) 的任何已完成结果；本文只规定"修复之后按什么顺序做什么"。
> 硬约束：代码截止 **2026-09-21 21:00**（剩 27 天），兼容性预检 **2026-09-17 21:00**，报告 **2026-09-28 21:00**。

---

## 0. 一页摘要

```text
冻结新的正式长训
  → P0 修复 + 官方生命周期集成测试        （阻断项，优先于一切）
  → P1 修复 + curriculum×E01 测试
  → 重跑 A02/A03/A05 三臂（重建定量基线）
  → 实验 1  T02 任务密度
  → 实验 2  A06 势函数塑形
  → 实验 3  n ∈ {1,3,5}
  → 实验 4  R04 replay ／ M3 MLP
  → M4 线   CNN + Double DQN（逐项加，不捆绑）
  → 9/17 submission test（硬门禁）
  → 9/21 提交
```

四条主线取代原 R01–R08 的八条：

| 主线 | 定义 | 回答的问题 | 状态 |
|---|---|---|---|
| **M1** | `R01` + `A03` + `E01` | 最小可解释基线 | ✅ 已完成 |
| **M2** | M1 + 势函数 + n-step + replay，训练任务 `T02` | 同一模型，训练配方能榨出多少 | 下一步 |
| **M3** | M2 但 `network = mlp_q` | 手工特征间有无非线性交互 | 待 M2 |
| **M4** | egocentric 棋盘张量 + Dueling Double DQN | 学到的空间表示能否超过手工特征 | 待 M3 |

课程要求的"至少两个不同的机器学习 agent"由 **M2/M3（课程方法线）+ M4（深度学习线）** 满足。

---

## 1. P0：terminal transition 双重更新（阻断项）

### 1.1 事实

[`runtime/experiment.py`](../agent_code/research_agent/runtime/experiment.py) 的 `end_round()` 无条件执行：

```python
self.observe(last_game_state, last_action, None, events, terminal=True)
```

而官方框架 [`environment.py`](../environment.py) 的 `do_step()` 顺序是：

```text
poll_and_run_agents()  → collect_coins / update_explosions / update_bombs / evaluate_explosions
                       → send_game_events()          ← game_events_occurred 在这里
                       → if time_to_stop(): end_round()   ← end_of_round 在这里
```

`Agent.reset_game_events()` 只在 `poll_and_run_agents()` 里调用，因此 `end_of_round` 收到的 `events` 是**同一份没有清空的列表**（额外多一个 `SURVIVED_ROUND`）。

### 1.2 两种回合结束路径不对称

`send_game_events()` 里是 `if a.train: if not a.dead: a.process_game_events(...)`，而 `a.dead` 在 `evaluate_explosions()`（更早）就已置位。因此：

| 情形 | `game_events_occurred` 收到 | `end_of_round` 收到 | 当前后果 |
|---|---|---|---|
| **存活到回合结束** | step `1..k` 全部 | step `k`（**重复**） | 同一 transition 被更新两次 |
| **死亡于 step k** | step `1..k-1` | step `k`（**全新**） | 正确，仅更新一次 |

**任何"发现重复就跳过 end_of_round"的修法都是错的**——死亡回合里 `end_of_round` 携带的是唯一一条带 `KILLED_SELF` / `GOT_KILLED` 的 transition，跳过它等于死亡惩罚 `D` 永远学不到，而 `D` 正是 §7.5 剂量-响应研究的唯一自变量。

### 1.3 证据

扫描 `runs/` 下全部 76 条 `round_end` 记录，其中 10 条为存活回合，**全部 `updates_this_round = act_steps + 1`**：

```text
segment02_coin_heaven round=2  act_steps=143 updates=144 diff=+1 coins=51 reward=51.0
segment02_coin_heaven round=4  act_steps=218 updates=219 diff=+1 coins=51 reward=51.0
segment02_coin_heaven round=7  act_steps=300 updates=301 diff=+1 coins=51 reward=51.0
```

`coin-heaven` 的 `COIN_COUNT = 50`，日志记录 **51**。最后一枚金币被计入两次。

### 1.4 修复规范

暂存一条 transition，延迟一步提交；`end_of_round` 用 `(round, step)` 判别是"重复"还是"全新"。

```python
# 伪代码。pending 保存 (old, action, new, events, key=(round, old["step"]))

def game_events_occurred(old, action, new, events):
    _commit_pending(terminal=False)          # 提交上一条：有 next_state，正常 bootstrap
    _stash(old, action, new, events)

def end_of_round(last, last_action, events):
    key = (last["round"], last["step"])
    if pending is not None and pending.key == key:
        # 存活结束：pending 就是这一条，从未提交过 → 提交且仅提交一次
        # events 用 end_of_round 的版本（是 pending.events 的超集，多 SURVIVED_ROUND）
        _commit(pending, events=events, terminal=terminal_on_truncation)
    else:
        # 死亡结束：pending 是 T_{k-1}，last 是从未送达的 T_k
        _commit_pending(terminal=False)
        _commit((last, last_action, None, events), terminal=True)
    pending = None
```

边界情形：

- agent 在 step 1 死亡 → `pending is None`，走 else 分支，正确；
- 存活分支里 `pending.new` 存在，因此 bootstrap 可行；
- 该结构与后续 n-step 所需的 transition 缓冲区同构，可直接扩展为长度 `n` 的 deque。

### 1.5 `terminal_on_truncation`：一个必须显式化、不能默认埋进代码的选择

`time_to_stop()`（[`environment.py:283`](../environment.py)）里存活结束有两种成因：

| 成因 | 语义 | 正确的 TD target |
|---|---|---|
| `step >= MAX_STEPS` | **时间上限截断**。世界没结束、agent 没死，MDP 本应继续 | **bootstrap**（Pardo et al. 2018, *Time Limits in RL*） |
| 只剩一个 agent 且无箱子、无可捡金币、无炸弹 | 未来真实回报确为 0 | bootstrap 与 terminal 近似等价 |

因此**默认 `terminal_on_truncation = false`**（即存活结束按截断处理、bootstrap），死亡结束仍是真终止（`target = r`）。

这一点对既有结论有直接影响：A01/A02 的坍塌形态是"全程 WAIT、恒 400 步"，**每一局都以截断收场**，当前代码给每局最后一步都压了一次 `target = r = 0` 的 terminal 更新，系统性地把"活到最后"的状态价值往 0 拉。修 P0 时若顺手把截断也当 terminal，等于把这个混淆项固化。

把它做成配置项，它本身就可以在有余力时当一次单因素消融。

### 1.6 必须新增的集成测试

现有 `test_runtime.py` 的 `test_r01_behaviour_is_hidden_behind_the_shared_runtime_interface` 用**不同**的 payload 调用 `observe` 和 `end_round`，恰好没覆盖这条路径。新增测试必须直接复刻官方生命周期：

| 测试 | 断言 |
|---|---|
| 存活到上限 | 末步 transition 恰好更新一次；末步的 `COIN_COLLECTED` 只计一次；`terminal=False` |
| 死亡于 step k | `T_{k-1}` 以 `terminal=False` 更新一次，`T_k` 以 `terminal=True` 更新一次；死亡惩罚恰好计一次 |
| step 1 即死亡 | 恰好一次 terminal 更新 |
| 存活回合金币计数 | `coin-heaven` 满盘 = 50，不是 51 |
| `terminal_on_truncation=true` | 存活末步改用 `target = r` |

### 1.7 修复后的端到端验证（2026-08-26）

修复后重跑 200 局 `coin-heaven` 训练（`A03` + `E01`，warm start）：

| 检查项 | 修复前 | 修复后 |
|---|---|---|
| 存活回合数 | — | 69 / 200 |
| `updates_this_round != act_steps` 的存活回合 | 全部 | **0** |
| 单局 `COIN_COLLECTED` 上限（地图只有 50 枚） | **51** | **50** |

单元测试：`agent_code/research_agent/tests/test_runtime.py::OfficialLifecycleTest` 共 8 项，覆盖 §1.6 全部条目。

### 1.8 对既有结果的处理

| 已完成实验 | 受影响程度 | 处理 |
|---|---|---|
| A02（恒 400 步存活） | **每局都中招** | 复跑 |
| A03（存活/死亡混合） | **部分中招** | 复跑 |
| A05（平均 15.6 步自爆） | 几乎不中招 | 复跑（保持三臂协议一致） |
| A00 / A01 / C01 / E01 | 不同程度 | **保留为方向性证据，不再作为最终数值比较** |

**三臂必须一起复跑**：三者受影响程度不同，只复跑一臂无法重建可比基线。复跑成本见 §5，不构成预算问题。

---

## 2. P1：curriculum × E01 会静默关闭 epsilon 衰减

[`run_experiment.py`](../scripts/run_experiment.py) 的 `_run_curriculum_segment()` 设置：

```python
"BOMBERMAN_TRAINING_ROUNDS": str(experiment.training.budget.rounds),   # 500
...
"--n-rounds", str(segment["rounds"]),                                   # 50
```

每个 segment 是**独立游戏进程，round 从 1 重新计数**。于是 `epsilon_for_training_round(round=1..50, training_rounds=500)`：`hold_rounds = 100`，round 恒 ≤ 100 ⇒ **整个 curriculum 全程 `epsilon = 0.30`，永不衰减，不报错、不留痕迹**。

反向路径：若某 segment 的 `rounds > budget.rounds`，`epsilon_for_training_round` 抛 `ValueError`，训练在中途崩溃。

C01 用 E00（常数），历史结果不受影响。但 [01 文档 §6.4](01_实验路线_奖励_课程训练设计.md) 的 C02 一旦配 E01 就会踩中。

**已实现**：新增 `curriculum.anneal_mode`，写入 config 与 snapshot，不靠默认值推断。

| 取值 | 语义 | 适用 |
|---|---|---|
| `global_round_offset`（默认） | 所有 segment 共享一条跨 segment 的退火曲线；worker 收到 `BOMBERMAN_ROUND_OFFSET` | C02 主线推荐 |
| `per_segment` | 每段独立退火（每段开头回弹到初始 epsilon） | 阶段切换需重新探索时 |

同时解除了原先"curriculum 必须搭配 E00"的配置限制——那条限制是在没有 offset 时的正确防御，现在组合已可正确运行。

配套测试：`scripts/tests/test_experiment_infra.py::CurriculumAnnealTest` 共 6 项，其中
`test_a_decaying_schedule_actually_decays_across_segments` 逐段推演真实 epsilon 序列，断言首值 `0.30`、末值 `0.05`、全程单调不增——这正是修复前会失败的性质。

---

## 3. 配置 schema 的最小增量

**不做 R 维度的大重命名。** 历史 run-id 与 `R01.x` 保持不变；`ExperimentConfig` 里 `network` / `algorithm` 已经分开，够用。只新增显式字段：

```json
{
  "agent": {
    "name": "research_agent",
    "model": "linear_q",
    "algorithm": "q_learning",
    "state_representation": "handcrafted_v1",
    "n_step": 1,
    "replay": null
  },
  "reward_version": "A03",
  "shaping": null,
  "exploration_version": "E01",
  "terminal_on_truncation": false,
  "curriculum": { "anneal_mode": "global_round_offset", "segments": [] }
}
```

规则不变：**新字段一律进 `resolved_runtime_config` 快照**，任何一次结果都能追回到产生它的完整取值；未实现的取值必须在 `require_implemented()` 里 fail closed，不得静默降级。

新增奖励版本：

```text
A06 = A03 + 势函数塑形（见 §4）
```

---

## 4. A06：势函数塑形规范

### 4.1 形式与保证

$$\hat r_{t+1}=r_{t+1}+\underbrace{\gamma\Phi(s_{t+1})-\Phi(s_t)}_{F(s_t,s_{t+1})}$$

按折扣求和望远镜化：

$$\sum_{t=0}^{T-1}\gamma^tF(s_t,s_{t+1})=\gamma^T\Phi(s_T)-\Phi(s_0)$$

令 $\Phi(\text{终止})=0$，整条轨迹的塑形贡献恒为 $-\Phi(s_0)$——**只与起点有关、与策略无关的常数**，故最优策略不变（Ng, Harada & Russell 1999）。

**精确表述**：$F$ 依赖 transition $(s,s')$；策略不变性成立**当且仅当** $F$ 能写成某个**只读状态的** $\Phi$ 的 $\gamma$-折扣差分。不是"不依赖 transition"。

**刷分的边界（诚实版本）**：来回横跳单次净收益为正（$\gamma=0.95$、$\Phi=-0.05d$ 时约 $+0.049$/来回），但**整条 episode 的塑形总和有界**，与横跳次数无关；而横跳会把所有真实奖励往后推、被 $\gamma^{\Delta t}$ 打掉。对比朴素塑形每个来回**净赚固定值、无上限**——这才是本质差别。

### 4.2 三条不可违反的实现约束

1. `Φ` 只读 `game_state`，**不得读 action、events 或历史**；
2. `Φ(终止) = 0`；
3. `Φ` 使用**与 learner 完全相同的 `γ`**（当前 `0.95`），且**有界**（BFS 距离必须 clip，否则 $\gamma<1$ 时有残余漂移）。

### 4.3 建议的 Φ

```python
def potential(game_state) -> float:
    if game_state is None:              # 终止
        return 0.0
    d = bfs_distance_to_nearest(reachable_targets(game_state))
    phi  = -0.05 * min(d, 20)                                    # 采集势
    phi += -0.30 if in_future_blast(game_state) else 0.0         # 危险势
    return phi
```

`reachable_targets` 的定义：

- 有可捡金币 → 金币格本身（金币格可通行）；
- 无可捡金币 → **可达且邻接箱子的合法站位**，不是箱子格本身（`field == 1` 不可通行，BFS 距离无定义）。直接复用 [`state.py`](../agent_code/research_agent/state.py) 已有的 `_crate_adjacent_cells()`。

**危险势是这一项的主要价值**：它在**状态层面**表达"我在不在未来爆炸范围内"，天然绕开 [01 文档 §7](01_实验路线_奖励_课程训练设计.md) 诊断出的 `CRATE_DESTROYED` 无法区分安全/自杀的结构性缺陷（`update_bombs` 早于 `evaluate_explosions`，自杀那步的 events 同时含两者）。因此 **A06 的优先级高于 A04 的事件级存活门控**：A04 仍在事件层打补丁，A06 在状态层一次解决且自带理论保证。

### 4.4 预注册

```text
对照：R01_A03_E01_C00_T02  n=1
处理：R01_A06_E01_C00_T02  n=1     ← 唯一变量：加入 Φ
```

预测：`approximate_safe_bomb_rate` 上升（`> 0.85`），`suicides` 下降，`wait_fraction` 不上升。
**反证条件**：若 `wait_fraction` 上升且 `bomb_rate` 下降，说明危险势权重 `-0.30` 过大、复制了 A02 的瘫痪形态，应先降权重而非加新项。

---

## 5. 实验排期

### 5.1 单次实验的真实成本（2026-08-25 实测，Apple M4）

| 项目 | Hetzner | 本机 M4 | 说明 |
|---|---:|---:|---|
| 训练 job（500 局 `classic`） | 28.8 s | **18.3 s** | 单核 1.64× |
| 评估 job（30 局 greedy） | 5.8 s | **3.25 s** | 单核 1.78× |
| 每 job 固定开销 | — | 0.40 s | python + numpy + pygame import |
| `copy_runtime` | — | 8–17 ms / 0.2 MB | allowlist 之后已可忽略 |
| **一次完整 run（5 训练 + 360 评估）串行** | ~40 min | **~21 min** | |
| **同上，10 路并行** | — | **~5 min** | 并行效率实测 4.5× |

因此 §1.7 的三臂复跑约 **15 分钟**（并行）／**1 小时**（串行）。**不构成预算约束。**

### 5.2 顺序

| 阶段 | 内容 | 单因素 | Gate | 预计 |
|---|---|---|---|---|
| **0** | **冻结新的正式长训** | — | — | — |
| **1** | 修 P0 + 官方生命周期集成测试 | — | §1.6 五项测试全绿 | 0.5 天 |
| **2** | 修 P1 + curriculum×E01 测试 | — | 多段 epsilon 端点正确 | 0.5 天 |
| **3** | 复跑 `A02/A03/A05` 三臂 | — | 重建定量基线；与旧结果的差异逐项说明 | 0.5 天 |
| **4** | **实验 1**：`A03_E01_C00_T02` | 只换 T | 有炸弹样本的 seed = 5/5；`crates_per_round` ↑ | 1 天 |
| **5** | **实验 2**：`A06_E01_C00_T02` | 只加 Φ | §4.4 预注册 | 1 天 |
| **6** | **实验 3**：`n ∈ {1,3,5}` | 只换 n | 选出 `n*` | 1 天 |
| **7** | **实验 4**：R04 replay ／ M3 MLP | 各自单因素 | 与 M2 对照 | 2 天 |
| **8** | M4：CNN smoke benchmark → 从零 Double DQN anchor → 逐项加 D4 / BC / Dueling | 逐项 | **9/14 未追平 M3 则冻结** | 8–10 天 |
| **9** | ⚠️ Docker 验证 + MaMPF submission test | — | **9/17 21:00 硬截止** | — |
| **10** | 多种子最终评测（原始框架、原始参数）、冻结、打包 | — | **9/21 21:00 硬截止** | — |

### 5.3 n-step 的载体与迁移边界

```text
主干：R01 + A06(或 A03) + E01 + C00 + T02
变量：n ∈ {1, 3, 5}
```

- **必须在 `T02` 上做**：`classic` 的 `bomb_rate` 仅 0.027、`rounds_with_bombs 13/30`，样本太稀测不出差异；`loot-crate` 的 `crates_per_round` 高一个量级。
- **候选值有物理依据**：炸弹 `timer=4` 于 $t$ 放下，`update_bombs` 在 $t{+}4$ 引爆，炸箱/露币事件落在放弹后**第 4–5 个 transition**。`n=1` 全靠 bootstrap，`n=5` 正好覆盖，`n=3` 取中点。
- **迁移边界（必须写进报告）**：最优 `n` 依赖函数近似误差大小，**不保证从线性迁移到 CNN**。CNN 稳定后只补跑一次 `n=1` vs `n*` 抽查，不重做全网格。

### 5.4 M4 的纪律

**不捆绑。** 顺序固定为：

```text
CNN 吞吐 smoke benchmark（先测，不外推 850 步/s）
  → 从零可学习的 Double DQN anchor
  → + egocentric 裁剪
  → + D4 对称增强
  → + BC 热启动（rule_based_agent 示范）
  → + Dueling
```

每一步单独记录，否则无法归因。**anchor 必须能从零学习**——若它本身学不动，后面所有增量都是在修补一个坏基座。

---

## 6. 工程遗留项（不阻断实验，按此优先级处理）

| 项 | 现状 | 建议 | 优先级 |
|---|---|---|---|
| ~~**runner 无并行**~~ | ✅ **已实现**：`run --jobs N`，阶段内并发、阶段间保序 | 27-job 探针实测 **75.8 s → 24.5 s（3.1×）**；除 `run_id`、绝对路径与 `inference_seconds` 外，结果与串行逐位一致 | 完成 |
| 磁盘 607 MB | 大头是 `framework_game.log`（2.7 MB/segment）与 `agent.jsonl`（2.5 MB/segment） | ① 训练 job 不再 `copy2` 框架 game log（它随 runtime 一起删，**零框架改动**）；② 加 `BOMBERMAN_ACTION_LOG_EVERY`，训练侧采样、评估侧全量（行为指标只从 30 局评估算） | 中 |
| 无回放 | runner 从不传 `--save-replay`（`main.py` 支持） | 只对 promotion 选中的 checkpoint、少数固定种子存回放，供报告定性分析 | 中 |
| 势函数/n-step 的测试 | 无 | Φ 的终止值、γ 一致性、望远镜和；n-step 的 target 展开 | 随实现 |

**并行下的一个计量注意事项**：`inference_seconds` 在 8 路并行时由 0.19 ms 升到约 0.37 ms（CPU 争用）。距 0.5 s 的官方上限仍有 1300 倍余量，不构成风险；但**报告里引用推理耗时时必须用串行测得的数值**，否则等于报告了一个受并发度影响的量。

**不建议**修改私有 runtime 的 `settings.py` 降日志级别。课程规则允许（只禁止最终 agent 依赖框架修改），但私有 runtime 成功后即删除，改动不会留在 provenance 里。上表的方案①效果相同且无此问题。

---

## 7. 待审核决议清单

审核时请对以下各项明确 **采纳／修改／驳回**：

1. P0 修法采用 §1.4 的暂存 + 判别器结构（而非"发现重复就跳过"）；
2. `terminal_on_truncation` 默认 `false`，且作为显式配置项而非硬编码；
3. A02/A03/A05 三臂全部复跑，旧结果降级为方向性证据；
4. P1 采用 `global_round_offset` 还是 `per_segment_anneal`；
5. 不做 R 维度重命名，只加 §3 的新字段；
6. A06 优先于 A04；
7. §5.2 的阶段顺序与 9/14 M4 冻结 gate；
8. §6 的 runner 并行化提到"高"优先级。
